# Astro Server Angular

Build `astro-server-angular` npm package from astro server OpenAPI specification.

## How to build

`mvn -Dastro-server-api.version=<astro-server-api-version> -Dpackage.version=<version-of-package> package`
